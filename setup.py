from setuptools import setup

version = '0.0.1'

setup(
    name='time_travel',
    version=version,
    description='Python time mocking',
    license='MIT',
    packages=('time_travel',),
    package_dir={'': 'src'},

)